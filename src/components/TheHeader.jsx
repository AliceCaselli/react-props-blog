import style from "../css/modules/TheHeader.module.css";

export default function TheHeader() {
    return (

        <header className={style.header}>
            <h1 className={style.h1}>TheBlog</h1>
        </header>
    );
}